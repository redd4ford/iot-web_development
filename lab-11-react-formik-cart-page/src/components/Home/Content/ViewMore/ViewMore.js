import React from 'react';

import "../ViewMore/ViewMore.css";

export default function ViewMore(props) {
    return(
        <div className="view-more-button">
            { border < items.length &&
                <button onClick={showMore}>View more elements</button>
            }
        </div>
    );
}
